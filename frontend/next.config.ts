import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // experimental: {
  //   globalNotFound: true ,//its a mthod to alow to global error
  // ppr:"incremental"
  // }
};

export default nextConfig;
